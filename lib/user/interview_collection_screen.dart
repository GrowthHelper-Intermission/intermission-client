import 'package:flutter/material.dart';
import 'package:intermission_project/common/component/custom_appbar.dart';

class InterviewCollectionScreen extends StatelessWidget {
  const InterviewCollectionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      //appBar: CustomAppBar(),
      body: Center(
        child: Text('InterviewCollectionScreen'),
      ),
    );
  }

}
