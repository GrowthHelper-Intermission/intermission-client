import 'package:flutter/material.dart';

class askInfo extends StatelessWidget {
  const askInfo({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
            'notification'
        ),
      ),
    );
  }
}